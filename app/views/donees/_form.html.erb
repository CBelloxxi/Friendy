<%= simple_form_for(@donee) do |f| %>
  <%= f.input :first_name %>
  <%= f.input :last_name %>
  <%= f.input :location, collection: ['London', 'Birmingham', 'Liverpool', 'Brighton', 'Newcastle', 'Blackpool'] %>
  <%= f.input :description %>
  <%= f.submit %>
<% end %>

<%= link_to "Back", donees_path %>

<%# need to add image to form and the qr code here%>
